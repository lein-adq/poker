- User must see multiple tables to explore
- User has to be able to join a unique table per account at a time
- Each table should allow a minimum and a maximum of each player chips stack
- Each table should start with at least 2 players
- Each table should support a max of 9 players
- When max players reached, the extra players incoming should be able to enter into a queue to enter when a player loses all their money
- A player should be able to charge chips on their stack any time but only effective when a round is done
- A player can enter as a spectator when no sit taken (default when entering into a game)
- User must be assigned 300 chips and a welcome gift claim of 300
- User must be gifted 300 chips daily (per user’s timezone at 06:00 of their local timezone)
- Users can chat to the table players (mark those which are spectating)
- Private tables? Unlimited chips not the user’s bag (unless explicitly told)

---

- Must be shown the chances of winning at every stage (flop, turn, river), important, when all hands are being shown (cases like ALL IN before ending the stages)
- A cheatsheet with all possible hands and their rank must be reachable from a question mark button to newbies incursion
- When all hands on the table are shown, a visual indicator should shown the hand made by each player below their hand (e.g. Player John Doe has a text below their hand saying “FLUSH” and so on)

---

- Auth with Email, only known domains to avoid temporal email (would be better and smarter to accept those most common ones, rather than blacklist the not allowed ones)
- No Safe For Work chat, but no assets or so able to send.